<div class="banner" align="left">
  <font size = "5" face="verdana" > MyProperty.com </font>
  <div align="right">
    <% if session[:user_name] %>
      <%= session[:user_name] %> | <%= link_to 'Logout', logout_path, method: :delete %>
    <% end %>
  </div>
</div>